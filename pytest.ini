[pytest]
testpaths = tests
# Every async test runs without needing a per-test marker, and no test in this
# suite is allowed to touch a network or a real provider — the whole point of
# injecting providers is that the suite runs offline and free.
asyncio_mode = auto
addopts = -q
