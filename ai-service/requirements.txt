fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2
numpy==2.1.1

# Only needed when VECTOR_STORE=mongo (Atlas Vector Search). Imported lazily so
# the stub/in-memory path runs without a database at all.
motor==3.6.0
pymongo==4.9.1

# dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
